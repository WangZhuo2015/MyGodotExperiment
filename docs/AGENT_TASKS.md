# Agent Tasks

Good next tasks for Antigravity or another coding agent:

- Improve snap solver so previews prefer surfaces under the cursor before nearest center distance.
- Add item hover outlines and stronger valid/invalid preview silhouettes.
- Add better placeholder pixel sprites generated from small indexed-color patterns.
- Add inventory and box opening animation.
- Add an Aseprite import pipeline that maps source layers to `ItemDef.sprite_path`.
- Add room transition support.
- Add multiple rooms and level selection.
- Add a surface editor helper that writes `SurfaceDef` data.
- Add undo/redo for placement changes.
- Add accessibility settings for preview colors and text scale.
- Add gamepad support.
- Add an audio interaction layer for pick up, rotate, valid drop, and invalid drop.
